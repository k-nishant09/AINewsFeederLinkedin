---
apiVersion: v1
kind: Service
metadata:
  name: news-mcp
  namespace: {{ .Values.global.namespace }}
  labels:
    app: news-mcp
    {{- include "aifeeders.labels" . | nindent 4 }}
spec:
  selector:
    app: news-mcp
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
