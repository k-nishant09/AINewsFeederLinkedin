---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: evaluation-mcp
  namespace: {{ .Values.global.namespace }}
  labels:
    app: evaluation-mcp
    role: mcp-server
    {{- include "aifeeders.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.evaluationMcp.replicas }}
  selector:
    matchLabels:
      app: evaluation-mcp
  template:
    metadata:
      labels:
        app: evaluation-mcp
        role: mcp-server
        {{- include "aifeeders.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: daily-news
      containers:
        - name: evaluation-mcp
          image: {{ include "aifeeders.image" (dict "registry" .Values.global.image.registry "name" .Values.evaluationMcp.image "tag" .Values.global.image.tag) }}
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: daily-news-config
            - secretRef:
                name: daily-news-secrets
          resources:
            {{- toYaml .Values.evaluationMcp.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 30
