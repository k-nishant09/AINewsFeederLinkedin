---
apiVersion: v1
kind: Service
metadata:
  name: pageindex-mcp
  namespace: {{ .Values.global.namespace }}
  labels:
    app: pageindex-mcp
    {{- include "aifeeders.labels" . | nindent 4 }}
spec:
  selector:
    app: pageindex-mcp
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
