---
apiVersion: v1
kind: Service
metadata:
  name: linkedin-mcp
  namespace: {{ .Values.global.namespace }}
  labels:
    app: linkedin-mcp
    {{- include "aifeeders.labels" . | nindent 4 }}
spec:
  selector:
    app: linkedin-mcp
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
